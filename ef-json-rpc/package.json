{
  "name": "json-rpc",
  "version": "1.0.0",
  "description": "ethereum foundation json rpc specification",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build.js",
    "watch": "watch -p '**/*.json' -c 'npm run build'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@apidevtools/json-schema-ref-parser": "^9.0.7",
    "@open-rpc/generator": "^1.18.12",
    "watch-cli": "^0.2.3"
  }
}
